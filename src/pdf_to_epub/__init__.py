"""PDF to EPUB V4 local-turbo baseline.

The package intentionally separates local OCR from optional AI validation so the
AI stage can be rerun without touching the expensive OCR result.
"""

__version__ = "0.4.0"
